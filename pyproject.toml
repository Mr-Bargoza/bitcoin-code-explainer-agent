[tool.poetry]
name = "bitcoin-code-explainer-agent"
version = "0.1.0"
description = "A Bitcoin code explainer agent."
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.8"

[build-system]
requires = ["poetry-core>=1.0,<2.0"]
build-backend = "poetry.core.masonry.api"